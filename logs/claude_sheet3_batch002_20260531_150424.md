# Sheet3 batch002 execution log

- **Time**: 2026-05-31 15:04:24 Asia/Shanghai (approx)
- **Supervisor**: Codex top-level; Claude Code middle agent used for packet-based reports.
- **Input genes**: ANP32D, ACBD6, ACYP1, ACYP2, AP5S1
- **Data packets**: `/Users/quii/Desktop/projects/TE-regulated proteins finding/protein_data/harvest_packets/{gene}.json`

## Harvest status summary

All 5 harvest packets show **complete failure** across all data sources. Every endpoint (UniProt, AlphaFold, PubMed, STRING, IntAct, HPA) returned `URLError: <urlopen error [Errno 8] nodename nor servname provided, or not known>` — a DNS resolution failure indicating no network connectivity during the harvest run.

## Results

| # | Gene | Final class | Report path | Status | HPA IF status | Missing data |
|---|---|---|---|---|---|---|
| 1 | ANP32D | rejected | `detail/rejected/ANP32D/ANP32D-evaluation.md` | rejected | IF unavailable — HPA URLError, no IF image | UniProt, AlphaFold, PubMed, STRING, IntAct, HPA — all failed |
| 2 | ACBD6 | rejected | `detail/rejected/ACBD6/ACBD6-evaluation.md` | rejected | IF unavailable — HPA URLError, no IF image | UniProt, AlphaFold, PubMed, STRING, IntAct, HPA — all failed |
| 3 | ACYP1 | rejected | `detail/rejected/ACYP1/ACYP1-evaluation.md` | rejected | IF unavailable — HPA URLError, no IF image | UniProt, AlphaFold, PubMed, STRING, IntAct, HPA — all failed |
| 4 | ACYP2 | rejected | `detail/rejected/ACYP2/ACYP2-evaluation.md` | rejected | IF unavailable — HPA URLError, no IF image | UniProt, AlphaFold, PubMed, STRING, IntAct, HPA — all failed |
| 5 | AP5S1 | rejected | `detail/rejected/AP5S1/AP5S1-evaluation.md` | rejected | IF unavailable — HPA URLError, no IF image | UniProt, AlphaFold, PubMed, STRING, IntAct, HPA — all failed |

## Summary

| Metric | Value |
|---|---|
| Total processed | 5 |
| Scored (nuclear) | 0 |
| Rejected | 5 |
| Harvest failures | 5/5 (100%) |

## Data quality assessment

**PubMed strict/broad**: All 0/0 (search failed)

**PPI coverage**: None — STRING, IntAct both failed

**IF images**: None acquired — HPA retrieval failed for all 5 genes

**Nuclear evidence**: Zero — UniProt and GO-CC both unavailable

**Root cause**: Harvest script ran without network connectivity (DNS unreachable). All 6 data sources per gene failed identically with `[Errno 8] nodename nor servname provided, or not known`.

## Rejected list

- ANP32D: no data available, requires re-harvest
- ACBD6: no data available, requires re-harvest
- ACYP1: no data available, requires re-harvest
- ACYP2: no data available, requires re-harvest
- AP5S1: no data available, requires re-harvest (known endosomal/lysosomal function, unlikely nuclear)

## Codex issues

- **Critical**: All 5 harvest packets are empty — harvest environment had no network connectivity at run time. These 5 genes require re-harvest with network access before meaningful evaluation can proceed.
- The harvest timestamps (15:03:07–15:03:53 on 2026-05-31) suggest the entire harvest batch ran without network. Check the harvest execution environment's network/DNS configuration.
